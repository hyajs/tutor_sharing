name: Build and Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: crpi-9nya4llb97sx949i.cn-hangzhou.personal.cr.aliyuncs.com
  BACKEND_IMAGE: crpi-9nya4llb97sx949i.cn-hangzhou.personal.cr.aliyuncs.com/hyajs/jiajiao-backend
  FRONTEND_IMAGE: crpi-9nya4llb97sx949i.cn-hangzhou.personal.cr.aliyuncs.com/hyajs/jiajiao-frontend
  POSTGRES_IMAGE: crpi-9nya4llb97sx949i.cn-hangzhou.personal.cr.aliyuncs.com/hyajs/jiajiao-postgres
  REDIS_IMAGE: crpi-9nya4llb97sx949i.cn-hangzhou.personal.cr.aliyuncs.com/hyajs/jiajiao-redis
  NGINX_IMAGE: crpi-9nya4llb97sx949i.cn-hangzhou.personal.cr.aliyuncs.com/hyajs/jiajiao-nginx
  TAG: ${{ github.sha }}

jobs:
  build-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Alibaba Cloud Container Registry
        run: |
          echo "${{ secrets.ACR_PASSWORD }}" | docker login ${{ env.REGISTRY }} --username ${{ secrets.ACR_USERNAME }} --password-stdin

      - name: Build and push backend image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.backend
          push: true
          tags: |
            ${{ env.BACKEND_IMAGE }}:${{ env.TAG }}
            ${{ env.BACKEND_IMAGE }}:latest

  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Alibaba Cloud Container Registry
        run: |
          echo "${{ secrets.ACR_PASSWORD }}" | docker login ${{ env.REGISTRY }} --username ${{ secrets.ACR_USERNAME }} --password-stdin

      - name: Build and push frontend image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.frontend
          push: true
          tags: |
            ${{ env.FRONTEND_IMAGE }}:${{ env.TAG }}
            ${{ env.FRONTEND_IMAGE }}:latest

  build-base-images:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Alibaba Cloud Container Registry
        run: |
          echo "${{ secrets.ACR_PASSWORD }}" | docker login ${{ env.REGISTRY }} --username ${{ secrets.ACR_USERNAME }} --password-stdin

      - name: Pull postgres image
        run: |
          docker pull postgres:15-alpine
          docker tag postgres:15-alpine ${{ env.POSTGRES_IMAGE }}:latest
          docker push ${{ env.POSTGRES_IMAGE }}:latest

      - name: Pull redis image
        run: |
          docker pull redis:7-alpine
          docker tag redis:7-alpine ${{ env.REDIS_IMAGE }}:latest
          docker push ${{ env.REDIS_IMAGE }}:latest

      - name: Pull nginx image
        run: |
          docker pull nginx:alpine
          docker tag nginx:alpine ${{ env.NGINX_IMAGE }}:latest
          docker push ${{ env.NGINX_IMAGE }}:latest

  deploy:
    needs: [build-backend, build-frontend, build-base-images]
    runs-on: ubuntu-latest
    steps:
      - name: Create nginx.conf on server
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.ECS_HOST }}
          username: root
          key: ${{ secrets.ECS_SSH_KEY }}
          script: |
            mkdir -p /root/projects/jiajiao/docker
            printf 'upstream backend {\n    server jiajiao-backend:8000;\n}\n\nupstream frontend {\n    server jiajiao-frontend:80;\n}\n\nserver {\n    listen 80;\n    server_name localhost;\n\n    # Proxy to frontend container for static files (SPA)\n    location / {\n        proxy_pass http://frontend;\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto $scheme;\n    }\n\n    # Proxy to backend for API\n    location /api/ {\n        proxy_pass http://backend/api/;\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto $scheme;\n    }\n}\n' > /root/projects/jiajiao/docker/nginx.conf
            echo "nginx.conf created"

      - name: Deploy containers on server
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.ECS_HOST }}
          username: root
          key: ${{ secrets.ECS_SSH_KEY }}
          envs: TAG
          script: |
            set -e
            export TAG=${{ env.TAG }}
            export REGISTRY=${{ env.REGISTRY }}
            export BACKEND_IMAGE=${{ env.BACKEND_IMAGE }}
            export FRONTEND_IMAGE=${{ env.FRONTEND_IMAGE }}
            export POSTGRES_IMAGE=${{ env.POSTGRES_IMAGE }}
            export REDIS_IMAGE=${{ env.REDIS_IMAGE }}
            export NGINX_IMAGE=${{ env.NGINX_IMAGE }}

            echo "${{ secrets.ACR_PASSWORD }}" | docker login $REGISTRY --username ${{ secrets.ACR_USERNAME }} --password-stdin

            docker network create jiajiao-net 2>/dev/null || true

            docker stop jiajiao-nginx jiajiao-frontend jiajiao-backend jiajiao-postgres jiajiao-redis 2>/dev/null || true
            docker rm jiajiao-nginx jiajiao-frontend jiajiao-backend jiajiao-postgres jiajiao-redis 2>/dev/null || true

            echo "Pulling images from ACR..."
            docker pull $BACKEND_IMAGE:$TAG
            docker pull $FRONTEND_IMAGE:$TAG
            docker pull $POSTGRES_IMAGE:latest
            docker pull $REDIS_IMAGE:latest
            docker pull $NGINX_IMAGE:latest

            docker tag $BACKEND_IMAGE:$TAG jiajiao-backend:latest
            docker tag $FRONTEND_IMAGE:$TAG jiajiao-frontend:latest
            docker tag $POSTGRES_IMAGE:latest postgres:15-alpine
            docker tag $REDIS_IMAGE:latest redis:7-alpine
            docker tag $NGINX_IMAGE:latest nginx:alpine

            echo "Starting PostgreSQL..."
            docker run -d \
              --name jiajiao-postgres \
              --restart=always \
              --network jiajiao-net \
              -e POSTGRES_USER=jiajiao \
              -e POSTGRES_PASSWORD=jiajiao123 \
              -e POSTGRES_DB=jiajiao \
              -v postgres_data:/var/lib/postgresql/data \
              postgres:15-alpine

            echo "Starting Redis..."
            docker run -d \
              --name jiajiao-redis \
              --restart=always \
              --network jiajiao-net \
              redis:7-alpine

            echo "Starting Backend..."
            docker run -d \
              --name jiajiao-backend \
              --restart=always \
              --network jiajiao-net \
              -p 8001:8000 \
              -e DATABASE_URL=postgresql://jiajiao:jiajiao123@jiajiao-postgres:5432/jiajiao \
              -e REDIS_URL=redis://jiajiao-redis:6379/0 \
              -e SECRET_KEY=your-secret-key-change-in-production \
              -e JWT_SECRET_KEY=your-jwt-secret-key-change-in-production \
              jiajiao-backend:latest

            echo "Starting Frontend..."
            docker run -d \
              --name jiajiao-frontend \
              --restart=always \
              --network jiajiao-net \
              --expose 80 \
              jiajiao-frontend:latest

            echo "Starting Nginx..."
            docker run -d \
              --name jiajiao-nginx \
              --restart=always \
              --network jiajiao-net \
              -p 81:80 \
              -v /root/projects/jiajiao/docker/nginx.conf:/etc/nginx/conf.d/default.conf \
              nginx:alpine

            echo ""
            echo "=== Deployment completed ==="
            docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"